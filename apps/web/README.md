# Web App

Vite + React web boundary for Test Flow AI.

The active Vite app still uses the root `src/` directory and root `vite.config.ts`.
Do not migrate this app to Next.js. Future web restructuring should preserve Vite and
move screens/components into this boundary incrementally.
