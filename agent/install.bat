@echo off
REM This TestFlow Agent bundle is SELF-CONTAINED — node_modules and the browser ship inside it.
REM You do NOT need to install anything.
echo ============================================================
echo   Nothing to install — this agent bundle is ready to run.
echo   Just double-click  start.bat  to launch the agent.
echo ============================================================
echo.
echo (Maintainers preparing a distributable bundle: use build-bundle.bat.)
pause
